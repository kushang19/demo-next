export default function ProductDeatilsLayout({children} : {children : React.ReactNode}){

    return (
        <div>
            {children}
            <h2>Feature Product</h2>
        </div>
    )

}